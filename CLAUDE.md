# AI作業場 — セッション効率化ルール（必読・毎セッション自動適用）

このフォルダには複数の占いビジネス関連プロジェクト（MIRAI_占い、ASUMIRA等）が同居している。
ブランドボイス・思想・禁止表現は `cloud.md` を、部署別の作業ルールは各部署フォルダの `rules.md` を参照する（無関係な部署のrules.mdは読み込まない）。

## セッションコスト削減ルール（6項目・全作業で徹底）

1. **大きいファイルは丸読みしない**
   - 編集対象は先にGrepで該当箇所を特定し、Readはoffset/limit指定で該当行の前後のみ読む。
   - 一度読んで把握済みの内容を、同一セッション内で理由なく全文読み直さない。

2. **ブラウザ検証は視覚確認が必須な時だけ行う**
   - レイアウト崩れ・見た目・アニメーションの確認 → 実ブラウザ/スクショ可。
   - 数値の整合性・クラス重複・テキスト内容の確認 → コード読み取り（Read/Grep）で完結させ、都度ブラウザは起動しない。

3. **案件・テーマが変わったら会話を分ける**
   - 明らかに別プロジェクト（例: 占いサイト → mira drop案件）に切り替わるタイミングでは、新規セッション開始または `/clear` を一言添えて提案する。

4. **指示はまとめて実行し、往復を減らす**
   - 着手前に関連しそうな追加修正点（レスポンシブ、SEO、alt属性等）を洗い出し、明らかに必要なものは都度確認を挟まず実施し、最後にまとめて報告する（Auto Mode方針に準拠）。

5. **広範囲の調査はExploreエージェントに委任する**
   - 3ファイル以上の横断調査・探索が必要な場合は本体コンテキストで行わず、Agent(Explore)に委任し、生ログを持ち込まない。

6. **恒久仕様はmemory / CLAUDE.md / cloud.mdに集約し、再説明を求めない**
   - 同じ仕様説明を毎回ユーザーに求めない。仕様変更があれば該当ファイルを都度更新する。
